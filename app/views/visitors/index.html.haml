%section.text-center
  %h1 Boussole des droits

  %h3
    Accède au conseil garanti d'un·e professionnel·le,

    - if feature?(:working) || feature?(:training) || feature?(:healthcaring)
      près de chez toi,

    pour t'aider dans tes projets

    - if feature?(:training)
      de formation

    - if feature?(:housing)
      de logement

    - if feature?(:working)
      d'activité

    - if feature?(:healthcaring)
      et de santé

%section.text-center
  - if feature?(:working)
    .col-md-2.col-md-offset-1
      .thumbnail
        = image_tag('work.png')
        .caption
          %h5 Travailler

  - if feature?(:training)
    .col-md-2
      = link_to(projects_path, method: :post) do
        .thumbnail
          = image_tag('education.png')
          .caption
            %h5 Me former

  - if feature?(:housing)
    - if feature?(:working) || feature?(:training) || feature?(:traveling) || feature?(:healthcaring)
      .col-md-2
        .thumbnail
          = link_to(housings_path, method: :post) do
            = image_tag('housing.png')
            .caption
              %h5 Me loger

    - else
      = link_to('Commencer !', housings_path, method: :post, class: 'submit btn-default')


  - if feature?(:traveling)
    .col-md-2
      .thumbnail
        = image_tag('travel.png')
        .caption
          %h5 Partir à l'étranger

  - if feature?(:healthcaring)
    .col-md-2
      .thumbnail
        = image_tag('health.png')
        .caption
          %h5 Me soigner
