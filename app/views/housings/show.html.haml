-# coding: utf-8

.col-md-7
  - @results.each do |public_service|
    %section.text-center.jumbotron
      %address
        %h3= public_service['title']
        / = public_service['description']
        = public_service['postal_address']

        - if public_service['phone']
          %br
          %abbr{ title: 'Téléphone' }
            T :
            = public_service['phone']

        - if public_service['email']
          %br
          %abbr{ title: 'Email' }
            E :
            = mail_to(public_service['email'])

        - if public_service['url']
          %br
          %abbr{ title: 'Site' }
            W :
            = link_to(public_service['url'], public_service['url'])

      %h5 Un·e professionnel·le est disponible pour t'aider à :

      .row
        - public_service['offer'].each do |service|
          .col-md-4
            %br
            %br
            .small
              = service

      - if public_service[:measures].presence

        %h5 
        %h5 Nous avons également détecté pour toi :

        - public_service[:measures].each do |mesure|
          %br
          %section.text-center
            %address
              %h4= mesure['title']
              / = mesure['description']
              - if mesure['url']
                %br
                %abbr{ title: 'Site' }
                  W :
                  = link_to(mesure['url'], mesure['url'])

.col-md-4.col-md-offset-1
  %section.jumbotron
    %legend Être contacté·e

    Souhaites-tu que cette ou ce professionnel·le te contacte suite à ta recherche ?
    %br
    %br

    = simple_form_for(Contact.new, url: contact_path, method: :post, wrapper: :vertical_form) do |f|
      .form-inputs
        = f.input_field :email_or_phone, placeholder: 'Email ou téléphone'

      %br
      .form-actions
        = link_to('Envoyer !', '#', class: 'submit btn-default')
