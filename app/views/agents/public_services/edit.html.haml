-# coding: utf-8

.l-container-narrow
  .l-content
    %article.profile-form
      %h1.profile-form-header= t('.heading')
      %hr

      = render(partial: 'form', locals: { public_service: @public_service, action: 'edit' })
