-# coding: utf-8

.l-container-wide
  .l-content
    %section.profile-list
      %h1.profile-list-header
        = link_to(@service_offering.title, agents_service_offering_path(@service_offering))
      %hr

      .profile-list-content
        %table
          %thead
            %tr
              %th Critère
              %th Public cible
              %th

          %tbody
            = simple_form_for([:agents, @service_offering, @target_public], wrapper: :vertical_form) do |f|
              %tr
                %td= 'Statut'
                %td= f.association(:statuses, label: false, label_method: :value, as: :check_boxes)
                %td= f.button :submit

              %tr
                %td= 'Disponibilité'
                %td= f.association(:engagements, label: false, label_method: :value, as: :check_boxes)
                %td= f.button :submit, 'Mettre à jour', class: 'btn btn-warning'
